
#include "ArbIOint.hh"

bool completar (Arbre<int> fa, algo mes) {
    if () condició inicial;
    
    else {
        int arrel = a.arrel();
        fills
        
        b1 = completar (a1, algo mes);
        b2 = completar (a2, algo mes);
        
        ai.plantar(arrel,a1,ad)
    }
}
